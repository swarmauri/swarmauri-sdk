I need you to generate a file named "{{ FILE.RENDERED_FILE_NAME }}". This file is part of the implementation for the "{{ PKG.NAME }}" package, which includes C++ extensions and uses the Meson build system.

Below are the details and requirements for this task:

1. **Purpose**:
   {{ FILE.EXTRAS.PURPOSE }}

2. **Description**:
   {{ FILE.EXTRAS.DESCRIPTION }}

3. **Requirements / Constraints**:
   {%- for requirement in FILE.EXTRAS.REQUIREMENTS %}
   - {{ requirement }}
   {%- endfor %}

4. **Style Guide**:
   - Python code must follow PEP 8 style guidelines.
   - C++ code must follow modern C++ best practices (C++17 or later).
   - All Python modules, functions, classes, and methods must include spacy-styled docstrings.
   - Python code must follow PEP 484 annotation guidelines.
   - C++ code must include comments explaining critical sections and logic.
   - Ensure proper handling of Python-C++ integration, including reference counting and NumPy API initialization.

5. **File and Code Preferences**:
   - Provide a fully functional implementation of the code.
   - For Python files:
     - Ensure compatibility with the C++ extension module.
     - Include examples of how to use the C++ functionality through the Python interface.
   - For C++ files:
     - Use the Python C API correctly.
     - Include at least one example function that demonstrates Python-C++ interaction.
     - Properly initialize the NumPy API if applicable.
   - For Meson build files:
     - Configure the build environment for the C++ extension module.
     - Include necessary dependencies and build options.

6. **Testing and Validation**:
   - Ensure the generated code is testable.
   - Include test cases or examples where applicable.

### Desired Output
Produce a single code snippet that contains the entire content for the file {{ FILE.RENDERED_FILE_NAME }}. 
Do not wrap it in triple backticks! Just provide the raw code ready to copy-paste.
Do not provide an explanation afterward.

{# At the bottom of the template, include any example files specified by the EXAMPLES array #}
{% if FILE.EXTRAS.EXAMPLES %}
#### 
Leverage the reference file below as a guideline to utilize when creating the new component.

{%- for example in FILE.EXTRAS.EXAMPLES %}
#### Reference File: `{{ example }}`
```
{% include example %}

```
{%- endfor %}
{% endif %}
 
### Example Format of the Output
<!-- Example only; your code will vary. This is to show how your response should look. -->
{% include FILE.FILE_NAME %}


{# At the bottom of the template, include any dependency files specified by the DEPENDENCIES array #}
{% if FILE.EXTRAS.DEPENDENCIES %}
### Dependencies
{%- for dependency in FILE.EXTRAS.DEPENDENCIES %}
#### `{{ dependency | replace(PROJ.ROOT + '/', '') }}`
```
{% include dependency %}

```

{%- endfor %}
{% endif %}


