# swarmauri_vectorstore_packagevectorizer

Standalone community package for a package metadata vector store with query-oriented CLI entrypoints.

## Status

- Package metadata is defined in `pyproject.toml`.
- CLI entrypoint scaffold: `pkgvs`.
- Vector store entrypoint: `swarmauri.vector_stores.PackageVectorStore`.
