# Vue runtime assets

Static files served by `layout_engine_atoms.runtime.vue.ManifestApp`. The entry
point is `index.html` with supporting modules in `src/`. Assets are published as
part of the Python package so the runtime can operate without an additional
build step.
